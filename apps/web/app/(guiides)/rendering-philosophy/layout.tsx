import React, {FC} from 'react';

const RenderingPhilosophyLayout: FC<{children: React.ReactNode}> = ({children}) => {
    return (
        <>
            {children}
        </>
    );
};

export default RenderingPhilosophyLayout;