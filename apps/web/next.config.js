/** @type {import('next').NextConfig} */
const nextConfig = {};

// CJS entry; `module` is provided by Node at runtime.
// eslint-disable-next-line no-undef -- Node CJS global
module.exports = nextConfig;
